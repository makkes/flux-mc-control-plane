apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: cluster-att
  namespace: flux-system
spec:
  interval: 1m0s
  prune: true
  path: ./clusters/att/remote
  kubeConfig:
    secretRef:
      name: cluster-att
  sourceRef:
    kind: GitRepository
    name: control-plane
